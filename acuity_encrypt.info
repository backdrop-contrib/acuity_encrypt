name = Encryption (Acuity Utils)
description = AES-256-GCM field encryption using PHP's built-in OpenSSL — no external libraries. Provides an encrypted text field type with click-to-reveal display formatter, and a public API for use by other Acuity modules.

type = module
backdrop = 1.x
package = Acuity Utilities
php = 8.0

configure = admin/config/acuity-utils/acuity_encrypt

maintainers[albancomputers] = Albany Computer Services

tags[] = Fields
tags[] = Security
tags[] = Utility
