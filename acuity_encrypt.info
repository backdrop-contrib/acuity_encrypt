name = Encryption (Acuity Utils)
description = AES-256-GCM field encryption using PHP's built-in OpenSSL — no external libraries. Encrypt any text field by selecting the Acuity Encrypt widget; includes click-to-reveal formatters, multi-slot key management with hard rotation, and a public API for other Acuity modules.

type = module
backdrop = 1.x
package = Acuity Utilities
php = 8.0

configure = admin/config/acuity-utils/acuity_encrypt

maintainers[albancomputers] = Albany Computer Services

tags[] = Fields
tags[] = Security
tags[] = Utility
